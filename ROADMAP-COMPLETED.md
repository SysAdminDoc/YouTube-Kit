# YouTube-Kit (Astra Deck) — Completed/Shipped Releases

## Shipped Releases

| Release | Headline | Key features |
|---------|----------|--------------|
| v3.7.0 | Transcript export, privacy hardening | Transcript export (Copy/.txt/.srt/LLM), expanded URL tracking-param strip (UTM + click IDs), `credentials: 'omit'` default in EXT_FETCH proxy |
| v3.8.0 | Toolbar popup, digital wellbeing, profiles | Toolbar popup w/ 15 quick-toggles + live filter, `settingsProfiles` save/load/import/export, `digitalWellbeing` break reminders + daily cap, `videoRotation`, `frameByFrameButtons`, `YTKIT_SETTING_CHANGED` live toggles |
| v3.9.0 | Visual filters, subtitle download, Reddit, diagnostics | `subtitleDownload` (SRT), `videoVisualFilters` (6-slider CSS filter panel), `dearrowPeekButton` (Alt-hold), `videoAgeColors`, `watchPageTabs`, `redditComments` (non-credentialed), `diagnosticLog`, `storageQuotaLRU`, `apiRetryBackoff` |
| v3.10.0 | Watch analytics, subtitle styling, AI summary, chapters | `watchHistoryAnalytics` (30-day CSS bar chart), `subtitleStyling` (caption CSS overrides), `aiVideoSummary` (BYO-key OpenAI/Anthropic/Gemini/Ollama), `copyChapterMarkdown`, `chapterJumpButtons` |

See `CHANGELOG.md` for full notes.

---

